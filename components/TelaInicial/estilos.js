import { StyleSheet } from 'react-native';

const estilos = StyleSheet.create({
    container: {
        flex: 1,
    },
    img:{
        width: 90,
        height: 100,
    },
    titulo:{
        color: 'black',
    }
});

export default estilos;